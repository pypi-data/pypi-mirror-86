# userblock_size

```
usage: userblock_size [-h] path

Get group or dataset userblock_size

positional arguments:
  path        Path to group or dataset

optional arguments:
  -h, --help  show this help message and exit
```
