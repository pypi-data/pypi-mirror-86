# libver

```
usage: libver [-h] path

Get group or dataset libver

positional arguments:
  path        Path to group or dataset

optional arguments:
  -h, --help  show this help message and exit
```
