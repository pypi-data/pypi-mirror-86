operationId: create_panel
parameters:
- description: NewPanel to be added.
  in: body
  name: data
  required: true
  schema:
    $ref: '#/definitions/NewPanel'

responses:
  '201':
    description: OK
    schema:
      $ref: '#/definitions/Panel'
  default:
    description: Error
    schema:
      $ref: '#/definitions/Error'
tags:
- assays