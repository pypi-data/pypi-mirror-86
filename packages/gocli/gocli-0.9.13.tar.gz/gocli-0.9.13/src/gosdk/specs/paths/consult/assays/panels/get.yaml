operationId: get_panels
responses:
  '200':
    description: List of Panel objects
    schema:
      $ref: '#/definitions/PanelList'
  default:
    description: unexpected error
    schema:
      $ref: '#/definitions/Error'
tags:
  - assays