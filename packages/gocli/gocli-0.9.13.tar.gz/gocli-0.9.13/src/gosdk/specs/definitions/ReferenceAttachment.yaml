allOf:
- $ref: '#/definitions/NewReferenceAttachment'
- properties:
    _refs:
      $ref: '#/definitions/Refs'
    created:
      format: date-time
      type: string
    documents:
      items:
        $ref: '#/definitions/Document'
      type: array
    id:
      format: int32
      type: integer
    reference_uuid:
      type: string
    updated:
      format: date-time
      type: string
    url_path:
      type: string
    uuid:
      type: string
  title: ReferenceAttachment
  type: object
