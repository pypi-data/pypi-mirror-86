operationId: get_classifications_detail
parameters:
- description: Retrieve 'classifications' record using 'uuid' field value.
  in: path
  name: pk
  required: true
  type: string
responses:
  '200':
    description: Classifications document response.
    schema:
      $ref: '#/definitions/ClassificationsDocument'
  default:
    description: unexpected error
    schema:
      $ref: '#/definitions/Error'
tags:
- classifications
