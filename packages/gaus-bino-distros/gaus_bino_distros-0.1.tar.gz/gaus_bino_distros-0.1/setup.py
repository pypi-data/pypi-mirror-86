from setuptools import setup

setup(name='gaus_bino_distros',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['gaus_bino_distros'],
      zip_safe=False)
